<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>New Figure</title>
  </head>
  <body>
    <form action="/figures" method="post">
      <label for="name">Name:</label>
      <input id="figure_name" type="text" name="figure[name]">
      <!-- <label for="figure[title_ids][]">Title ID:</label> -->
      <!-- <input type="integer" name="figure[title_ids][]"> -->
      <% Title.all.each do |title| %>
      <input type="checkbox" name="figure[title_ids][]" value="<%= title.id %>" id="title_<%= title.id %>">
      <% end %>
      <% Landmark.all.each do |landmark| %>
      <input type="checkbox" name="figure[landmark_ids][]" value="<%= landmark.id %>" id="landmark_<%= landmark.id %>">
      <% end %>
      <button type="submit" name="submit">Create New Figure</button>
    </form>
  </body>
</html>
